// All of these INFRASTRUCTURE FILES TOGETHER WILL FORM OUR THEME.
import { colors } from "./colors";
import { space, lineHeights } from "./spacing";
import { sizes } from "./sizes";
import { fonts, fontWeights, fontSizes } from "./fonts";
// Above we have imported all of our infrastructure files together.
// Now we can use them in our theme.

export const theme = {
  colors,
  space,
  lineHeights,
  sizes,
  fonts,
  fontSizes,
  fontWeights,
};